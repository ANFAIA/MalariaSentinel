# Resume Protocol

After completing a fork negotiation:

1. Pop the top frame from the frame stack.
2. Emit: [resume] back to '{goal}' — step {N}/{total} ({next_step})
3. Continue from where you left off.
4. If frame stack is empty, return to the original goal.
