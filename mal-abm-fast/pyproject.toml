[project]
name = "mal-abm-fast"
version = "0.1.0"
description = "Fast C++ ABM engine for MalariaSentinel Centinela (M-perf milestone)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
