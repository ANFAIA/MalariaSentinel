[project]
name = "mal-abm-fast"
version = "0.1.0"
description = "Fast C++ ABM engine for MalariaSentinel Centinela (M-perf milestone)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai>=1.4.0",
]

[tool.uv]
package = false
